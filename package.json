{
  "name": "scoutx-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ kushalbery/scoutx-subgraph",
    "create-local": "graph create --node http://localhost:8020/ kushalbery/scoutx-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ kushalbery/scoutx-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 kushalbery/scoutx-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.26.0",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}
